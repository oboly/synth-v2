# A+ Legacy Lane

This directory contains the old A+ parsing lane.

Why archived:
- built for an older semi-structured A+ output format
- not aligned with the current table-based Breathline consistency runs
- kept only for historical reference and possible one-time migration support

Do not extend this lane for new work.

Active replacement:
- build and use `src/breathline/` for the current clean table-based pipeline
